spring:
  application:
    name: processing

  # ── RabbitMQ ──────────────────────────────────────────────
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  # ── MongoDB ───────────────────────────────────────────────
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/processing}
  
  # For newer Spring Boot versions that might have changed the property path
  mongodb:
    uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/processing}

# ── Custom App Properties ────────────────────────────────
app:
  rabbitmq:
    exchange: petitions-exchange
    queue: petitions-queue
    routing-key: petition.created
